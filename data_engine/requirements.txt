# Common
datasets>=2.0
tqdm>=4.0

# render/  (LaTeX -> PDF -> PNG)
pymupdf>=1.20
pdf2image>=1.16
pdfCropMargins>=2.0
Pillow>=9.0

# filtering/
numpy>=1.20
transformers>=4.30

# normalize/  (OpenAI-compatible LLM API)
openai>=1.0

# annotation/  (vLLM batched VLM inference; install on a CUDA machine)
pandas>=1.3
pyarrow>=10.0
vllm>=0.5
qwen_vl_utils
